server:
  port: 8081
spring:
  application:
    name: consul-demo
  cloud:
    consul:
      host: 127.0.0.1  # consul ??????
      port: 8500   # consul ??????
      enabled: true # ????consul
      # ???????
      discovery:
        register: true # ??????
        instance-id: ${spring.application.name}-01  # ????ID, ????
        service-name: ${spring.application.name} # ????
        port: ${server.port} # ????
        prefer-ip-address: true # ????IP????
        ip-address: ${spring.cloud.client.ip-address} # ????IP

